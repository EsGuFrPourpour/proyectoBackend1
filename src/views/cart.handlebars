<div class="cart-container">
  <div class="cart-header">
    <h1>🛒 Mi Carrito</h1>
    <p class="cart-subtitle">Revisa tus productos antes de finalizar la compra</p>
  </div>

  <!-- Estructura siempre visible para carga dinámica -->
  <div class="cart-content">
    <div class="cart-items">
      <!-- Los productos se cargarán dinámicamente aquí -->
      {{#each cart.products}}
        <div class="cart-item" data-product-id="{{this.product._id}}">
          <div class="item-image">
            <img src="{{#if this.product.thumbnails.[0]}}{{this.product.thumbnails.[0]}}{{else}}/images/no-image.png{{/if}}" 
                 alt="{{this.product.title}}" 
                 data-fallback="/images/no-image.png">
          </div>
          
          <div class="item-details">
            <h3 class="item-title">{{this.product.title}}</h3>
            <p class="item-description">{{this.product.description}}</p>
            <p class="item-price">${{this.product.price}}</p>
          </div>
          
          <div class="item-quantity">
            <button class="quantity-btn decrease" data-action="decrease-quantity" data-product-id="{{this.product._id}}">-</button>
            <span class="quantity-display">{{this.quantity}}</span>
            <button class="quantity-btn increase" data-action="increase-quantity" data-product-id="{{this.product._id}}">+</button>
          </div>
          
          <div class="item-total">
            <span class="item-subtotal">${{multiply this.product.price this.quantity}}</span>
          </div>
          
          <div class="item-actions">
            <button class="remove-btn" data-action="remove-from-cart" data-product-id="{{this.product._id}}">
              🗑️ Eliminar
            </button>
          </div>
        </div>
      {{/each}}
    </div>
    
    <div class="cart-summary">
      <div class="summary-card">
        <h3>Resumen del Pedido</h3>
        <div class="summary-line">
          <span>Subtotal:</span>
          <span class="cart-total">$0</span>
        </div>
        <div class="summary-line">
          <span>Envío:</span>
          <span>Gratis</span>
        </div>
        <div class="summary-line total-line">
          <span><strong>Total:</strong></span>
          <span class="cart-total"><strong>$0</strong></span>
        </div>
        
        <div class="checkout-actions">
          <button class="btn-checkout" data-action="checkout">
            💳 Proceder al Pago
          </button>
          <a href="/products" class="btn-continue-shopping">
            ← Seguir Comprando
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Mensaje de carrito vacío oculto por defecto -->
  <div class="empty-cart" style="display: none;">
    <div class="empty-cart-icon">🛒</div>
    <h2>Tu carrito está vacío</h2>
    <p>¡Agrega algunos productos para comenzar!</p>
    <a href="/products" class="btn-start-shopping">
      🛍️ Comenzar a Comprar
    </a>
  </div>
</div>

<div id="notification" class="notification hidden">
  <span id="notification-message"></span>
</div>

<style>
.cart-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}

.cart-header {
  text-align: center;
  margin-bottom: 30px;
}

.cart-header h1 {
  color: #333;
  margin-bottom: 10px;
}

.cart-subtitle {
  color: #666;
  font-size: 16px;
}

.cart-content {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 30px;
  margin-top: 20px;
}

.cart-items {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.cart-item {
  display: grid;
  grid-template-columns: 100px 1fr auto auto auto;
  gap: 20px;
  padding: 20px;
  border: 1px solid #ddd;
  border-radius: 8px;
  background: white;
  align-items: center;
}

.item-image img {
  width: 80px;
  height: 80px;
  object-fit: cover;
  border-radius: 4px;
}

.item-details h3 {
  margin: 0 0 5px 0;
  color: #333;
}

.item-description {
  color: #666;
  font-size: 14px;
  margin: 5px 0;
}

.item-price {
  font-weight: bold;
  color: #007bff;
  margin: 5px 0 0 0;
}

.item-quantity {
  display: flex;
  align-items: center;
  gap: 10px;
}

.quantity-btn {
  width: 30px;
  height: 30px;
  border: 1px solid #ddd;
  background: white;
  border-radius: 4px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}

.quantity-btn:hover {
  background: #f5f5f5;
}

.quantity-display {
  min-width: 30px;
  text-align: center;
  font-weight: bold;
}

.item-subtotal {
  font-weight: bold;
  color: #333;
  font-size: 16px;
}

.remove-btn {
  background: #dc3545;
  color: white;
  border: none;
  padding: 8px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
}

.remove-btn:hover {
  background: #c82333;
}

.summary-card {
  background: white;
  border: 1px solid #ddd;
  border-radius: 8px;
  padding: 20px;
  height: fit-content;
}

.summary-card h3 {
  margin-top: 0;
  color: #333;
}

.summary-line {
  display: flex;
  justify-content: space-between;
  margin: 10px 0;
  padding: 5px 0;
}

.total-line {
  border-top: 1px solid #ddd;
  margin-top: 15px;
  padding-top: 15px;
  font-size: 18px;
}

.checkout-actions {
  margin-top: 20px;
}

.btn-checkout {
  width: 100%;
  background: #28a745;
  color: white;
  border: none;
  padding: 12px;
  border-radius: 4px;
  font-size: 16px;
  cursor: pointer;
  margin-bottom: 10px;
}

.btn-checkout:hover {
  background: #218838;
}

.btn-continue-shopping {
  display: block;
  text-align: center;
  color: #007bff;
  text-decoration: none;
  padding: 8px;
}

.btn-continue-shopping:hover {
  text-decoration: underline;
}

.empty-cart {
  text-align: center;
  padding: 60px 20px;
}

.empty-cart-icon {
  font-size: 64px;
  margin-bottom: 20px;
}

.empty-cart h2 {
  color: #333;
  margin-bottom: 10px;
}

.empty-cart p {
  color: #666;
  margin-bottom: 30px;
}

.btn-start-shopping {
  display: inline-block;
  background: #007bff;
  color: white;
  text-decoration: none;
  padding: 12px 24px;
  border-radius: 4px;
  font-size: 16px;
}

.btn-start-shopping:hover {
  background: #0056b3;
}

.notification {
  position: fixed;
  top: 20px;
  right: 20px;
  background: #28a745;
  color: white;
  padding: 12px 20px;
  border-radius: 4px;
  z-index: 1000;
  transition: all 0.3s ease;
}

.notification.hidden {
  opacity: 0;
  transform: translateX(100%);
}

.notification.error {
  background: #dc3545;
}

@media (max-width: 768px) {
  .cart-content {
    grid-template-columns: 1fr;
  }
  
  .cart-item {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 15px;
  }
  
  .item-image {
    justify-self: center;
  }
}
</style>

<script src="/js/cart.js"></script>
